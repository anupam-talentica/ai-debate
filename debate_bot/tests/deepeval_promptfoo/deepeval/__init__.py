"""deepeval option (C1) — GEval rubric metrics scored by a Claude judge.

NOTE: this local package is named ``deepeval`` to match the plan's layout, but
Python's absolute imports mean ``import deepeval`` inside these modules still
resolves to the installed pip package, not this directory.
"""
