# Real-Time Multi-Agent Debate Chatbot — MVP

A structured debate between three LangGraph agents (Pro, Con, Moderator) with in-memory vector storage for cross-debate memory evolution.

## Setup

```bash
cp .env.example .env          # add your ANTHROPIC_API_KEY
pip install -r requirements.txt
jupyter notebook debate.ipynb
```
