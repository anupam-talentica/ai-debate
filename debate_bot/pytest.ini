[pytest]
addopts = -m "not eval"
markers =
    e2e: marks tests as end-to-end tests (deselect with '-m "not e2e"')
    eval: marks LLM eval tests that hit the real model (deselected by default; run with '-m eval')
    asyncio: marks tests as async tests
