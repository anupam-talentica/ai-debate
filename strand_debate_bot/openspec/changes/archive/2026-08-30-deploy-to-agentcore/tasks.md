## 1. AWS infrastructure

- [x] 1.1 Create an S3 bucket for session checkpoints (design.md, Decision 4) — `debate-bot-agentcore-sessions-557926277200` (ap-south-1), public access blocked, default SSE-AES256
- [x] 1.2 Create an S3 Vectors bucket and index for cross-debate memory (design.md, Decision 5) — dimension/distance-metric matching the embedding model chosen in task 3.2 — bucket `debate-bot-agentcore-memory`, index `debates`, dimension 1024 (`amazon.titan-embed-text-v2:0`'s actual output size, confirmed by a real `invoke_model` call), cosine distance
- [x] 1.3 Create an ECR repository for the container image — `debate-bot-agentcore` (ap-south-1), scan-on-push enabled
- [x] 1.4 Create an IAM execution role scoped to: the session bucket, the S3 Vectors bucket/index, ECR pull, CloudWatch logs, and (if kept) Secrets Manager read for `ANTHROPIC_API_KEY` — `debate-bot-agentcore-execution-role`, trust policy + permission policy per AWS's documented AgentCore Runtime execution-role template, scoped resources only (this repo, this bucket, this vector index) rather than the wildcard patterns the docs show as examples. Used `environmentVariables` on `create_agent_runtime` for `ANTHROPIC_API_KEY` instead of Secrets Manager (confirmed supported directly on the API) — simpler for now, matching the `.env`-based pattern already used everywhere else in this codebase; worth revisiting for stronger secret hygiene later
- [x] 1.5 Add the new config (bucket names, S3 Vectors index name, region) to `src/core/config.py`, following the existing `os.environ.get(...)` pattern

## 2. Durability backend: S3SessionManager (design.md, Decision 4)

- [x] 2.1 Rewrite `src/core/session.py`'s `save_invocation_state`/`load_invocation_state` to call `S3SessionManager`'s `create_multi_agent`/`update_multi_agent`/`read_multi_agent` instead of `tempfile`/`os.replace` against local disk — done via a plain `boto3` S3 client at the same sibling-object key `S3SessionManager` would use internally (`session_<run_id>/invocation_state.json`), rather than routing through the SessionManager's own repository interface, since that interface has no method for an ad hoc sibling file — matches how the local path already bypasses `FileSessionManager` for this exact reason
- [x] 2.2 Rewrite `read_persisted_graph_status` to read the equivalent status/interrupt fields from S3 (`session_<run_id>/multi_agents/multi_agent_default_graph/multi_agent.json`, mirroring `S3SessionManager`'s own private key layout and `Graph`'s private `_DEFAULT_GRAPH_ID`) instead of glob-reading `multi_agent.json` off local disk
- [x] 2.3 Update `build_graph()` (`src/core/graph.py`) to construct an `S3SessionManager(session_id=run_id, bucket=..., prefix=...)` in place of `FileSessionManager(session_id=run_id, storage_dir=...)` when `config.S3_SESSION_BUCKET` is set, gated the same way otherwise (only when `run_id is not None`) — both backends coexist, selected by config, per task 2.6
- [x] 2.4 Confirm `InvocationStatePersistenceHook`'s trigger points (`AfterNodeCallEvent`, `AfterMultiAgentInvocationEvent`) and its role as a sibling side-channel are unchanged — only its storage calls move (verified: the hook class itself has zero edits; the branch lives inside `save_invocation_state()`, which the hook already calls)
- [ ] 2.5 Add an integration test exercising `session_exists`/`save_invocation_state`/`load_invocation_state`/`read_persisted_graph_status` against a real (or LocalStack) S3 bucket, parallel to the existing `tests/test_durability.py` coverage of the local-disk path — DEFERRED: real AWS access was obtained later in this change (S3 bucket `debate-bot-agentcore-sessions-557926277200` is live) and the mechanism itself was proven manually via a real `StopRuntimeSession` + resume-from-S3 cycle against the deployed AgentCore Runtime, but no automated `pytest` integration test was written against it; to be handled later
- [x] 2.6 Confirm existing local-mode tests (`tests/test_durability.py`, `tests/test_durability_subprocess.py`) still pass unmodified — they exercise `FileSessionManager`, which remains the default when no AWS backend is configured (verified: full suite including the subprocess test — 57/57 pass, zero edits to any test file)

## 3. Memory backend: S3VectorMemoryStore (design.md, Decision 5)

- [x] 3.1 Implement `S3VectorMemoryStore` in `src/core/memory.py`, matching `ChromaMemoryStore`'s `strands.memory.types.MemoryStore` shape (`search`/`add`, `writable`/`extraction`/`max_search_results` attributes) — the class replaces `ChromaMemoryStore`, called from the exact same `search`/`add` sites in `src/agents/moderator.py`, no change to when memory is used. S3 Vectors has no separate document-body field, so `content` rides inside the vector's own `metadata["content"]` and is popped back out on `search`
- [x] 3.2 Choose and wire an embedding model for `add`/`search` (a Bedrock embedding model matching `ChromaMemoryStore`'s current MiniLM quality class — design.md, Open Questions) via `boto3` — chose `amazon.titan-embed-text-v2:0` (configurable via `BEDROCK_EMBEDDING_MODEL_ID`), called through `bedrock-runtime`'s `invoke_model`
- [x] 3.3 Implement `add` using S3 Vectors' `put_vectors` API, storing the debate summary text as metadata alongside its embedding
- [x] 3.4 Implement `search` using S3 Vectors' `query_vectors` API, returning `MemoryEntry` objects capped at `max_search_results`, most-similar first
- [x] 3.5 Confirm the failure-degrades-to-empty-context behavior (`cross-debate-memory`'s "Memory retrieval failure never blocks a debate" requirement) holds for this backend — verified: the try/except lives in `ModeratorHub._retrieve_memory_context` (`src/agents/moderator.py`), one level above any `MemoryStore` implementation, so it already applies to `S3VectorMemoryStore` with no changes needed; `S3VectorMemoryStore.search` deliberately raises naturally on failure rather than swallowing its own errors, same as `ChromaMemoryStore`
- [x] 3.6 Update `build_graph()`'s `_default_memory_store()` (or its call site) to select `S3VectorMemoryStore` vs. `ChromaMemoryStore` based on the AgentCore-vs-local config, mirroring how the session-manager choice is gated
- [ ] 3.7 Add an integration test exercising `add` then `search` against a real (or LocalStack-equivalent) S3 Vectors index, parallel to existing Chroma-backed memory test coverage (`tests/test_cross_debate_memory.py`) — DEFERRED: real AWS access was obtained later in this change (S3 Vectors bucket `debate-bot-agentcore-memory`, index `debates`, is live) but no automated `pytest` integration test was written against it, and full verdict-completion verification (which exercises memory add/search) remains blocked on Anthropic API credit per the user's explicit choice; to be handled later
- [x] 3.8 Confirm existing local-mode tests (`tests/test_memory.py`, `tests/test_cross_debate_memory.py`) still pass unmodified — they exercise `ChromaMemoryStore`, which remains the default when no AWS backend is configured (verified in the same full-suite run as task 2.6)

## 4. AgentCore invocation dispatcher (design.md, Decision 1; specs/agentcore-deployment)

- [x] 4.1 Create a new entrypoint module exposing `POST /invocations` and `GET /ping` on port 8080, alongside (not replacing) `src/api/routes/debates.py`'s existing router — `src/api/routes/agentcore.py`, mounted in `app.py`
- [x] 4.2 Implement `GET /ping` as a plain liveness check, independent of any run's state (specs/agentcore-deployment, "Liveness check independent of any debate run")
- [x] 4.3 Implement `POST /invocations` dispatch: read an `action` field (`start`, `stream`, `submit_audience_question`, `resume`, `invoke`) from the payload and call the corresponding existing `DebateService` method — no new debate logic, pure translation (specs/agentcore-deployment, "Single dispatched invocation endpoint")
- [x] 4.4 Return an explicit error response (not a crash or silent no-op) for an unrecognized `action` value (specs/agentcore-deployment, "An unrecognized operation is rejected explicitly")
- [x] 4.5 Use the incoming AgentCore session identifier (`runtimeSessionId`, carried per the AgentCore HTTP protocol's session header) as the `run_id` passed to `DebateService`, so one debate maps 1:1 to one AgentCore session (design.md, Decision 2) — required adding an optional `run_id` param to `DebateService.start()`/`.invoke()` (both previously always minted their own `uuid.uuid4()`), defaulting to the old behavior so existing REST routes are unaffected
- [x] 4.6 For the `stream` action, adapt `DebateService.stream_registered`'s SSE-event async generator into whatever streaming response shape AgentCore's `/invocations` expects — implemented as a `StreamingResponse` with `media_type="text/event-stream"`, the same contract `/debate/stream/{run_id}` already uses; verified manually end-to-end against real Anthropic calls (not just fakes) that this streams correctly through FastAPI. Flagged as worth re-confirming against a real AgentCore-hosted invocation once deployed (task 6.1) — the docs' own streaming example uses the SDK's `@app.entrypoint` async-generator wrapper (Option A), not the custom-FastAPI path (Option B) this project uses, so the exact wire format AgentCore expects for a custom app's streaming response is not fully confirmed from docs alone
- [x] 4.7 Add tests for the dispatcher itself: valid actions route to the right `DebateService` call, an unknown action is rejected explicitly, and two different session identifiers' requests don't cross-contaminate (specs/agentcore-deployment, "Concurrent debate runs under different sessions remain isolated") — `tests/test_agentcore.py`, 6 tests, reusing `tests/fakes.py`'s `FakeModel`/`FakeMemoryStore`; full suite (`tests/` + this file) is 63/63 passing

## 5. Containerization and deployment (design.md, Decision 6 and Migration Plan)

- [x] 5.1 Write a `Dockerfile`: `linux/arm64` base, install `requirements.txt` plus `boto3`, expose port 8080, run the new entrypoint via `uvicorn`
- [x] 5.2 Build and test the image locally (`docker build --platform linux/arm64`, run with AWS credentials passed through, `curl /ping` and `curl /invocations`) — built and ran on this machine's native arm64 Docker (no emulation needed); `/ping` returned healthy, `/invocations` correctly read the session header and dispatched `start`/`stream`, streaming real SSE events out of the running container. (Used `MOCK_LLM=true` to avoid a real Anthropic charge for this smoke test; it failed at the mock-transcript-fixture step since `tests/` isn't copied into the image by design — expected and correct, not a bug. The dispatcher's real, non-mock path was already verified end-to-end with live Anthropic calls outside the container in task 4.6/4.7's manual check.)
- [x] 5.3 Push the image to the ECR repository from task 1.3 — pushed to `557926277200.dkr.ecr.ap-south-1.amazonaws.com/debate-bot-agentcore:latest`
- [x] 5.4 Deploy via `create_agent_runtime`, setting `lifecycleConfiguration.idleRuntimeSessionTimeout` to a multi-hour value (design.md, Decision 3) and `roleArn` to the execution role from task 1.4 — deployed as `debate_bot_agentcore-C3ZGzNGB5V` (`agentRuntimeName="debate_bot_agentcore"` — AgentCore runtime names don't accept hyphens), `idleRuntimeSessionTimeout=14400` (4h), `maxLifetime=28800` (8h, the microVM ceiling), `networkMode=PUBLIC`. Had to correct the IAM policy's log-group/workload-identity resource patterns after creation once the actual assigned runtime name was known (it used underscores, not the hyphenated name my initial policy guessed at)
- [x] 5.5 Enable AgentCore Observability (CloudWatch/OTel) per PRD G8 — CloudWatch Transaction Search was already enabled account-wide (100% sampling, pre-existing from other AgentCore work in this account); added `aws-opentelemetry-distro` to `requirements.txt` and changed the `Dockerfile`'s `CMD` to `opentelemetry-instrument uvicorn app:app ...` (Strands' documented Custom Agent/Option B pattern). Rebuilt, smoke-tested locally, pushed, and `update_agent_runtime`'d the deployed runtime to version 2 to pick it up

## 6. End-to-end verification

Ran against the real deployed AgentCore Runtime (`debate_bot_agentcore-C3ZGzNGB5V`, ap-south-1). The Anthropic key configured in this environment has no credit, so live Pro/Con turns weren't possible — verified everything achievable for free with `MOCK_LLM=true` (mocked turns need no model call; a one-off test image temporarily added `tests/mock_cache/` fixtures, not part of the production `Dockerfile`) and reasoned explicitly about what that does and doesn't cover. Runtime restored to its real production config (real image, real `ANTHROPIC_API_KEY`, `idleRuntimeSessionTimeout=14400`/`maxLifetime=28800`) afterward.

- [x] 6.1 Start a debate against the deployed AgentCore Runtime; confirm it streams through opening and rebuttal — confirmed for real: `start` → `stream` returned `moderator, pro_opening, con_opening, moderator, pro_rebuttal, con_rebuttal, moderator, AWAITING_AUDIENCE_QUESTION`
- [x] 6.2 Submit an audience question after a deliberate delay (long enough to plausibly exceed a short test `idleRuntimeSessionTimeout`, to exercise the Stopped/resume path from design.md, Decision 3); confirm the debate resumes and reaches a completed verdict — the Stop/resume half is confirmed for real, against genuine AgentCore infrastructure, not a simulation: called `StopRuntimeSession` directly on the live session (a real microVM teardown), then `action: "resume"` on the same `runtimeSessionId` — a fresh compute correctly read the S3-persisted checkpoint and reported the run still `AWAITING_AUDIENCE_QUESTION`, exactly as design.md's Decision 3 predicted. **Not verified**: reaching a *completed verdict* after that — `submit_audience_question` calls `pro_addresses_question`/`con_addresses_question`, which are deliberately never mocked (novel content each run), so this needs real Anthropic credit. The equivalent full-completion path (with fakes, not live Anthropic) is covered by `tests/test_agentcore.py::test_full_lifecycle_through_the_dispatcher_reaches_a_verdict`
- [x] 6.3 Confirm two debates started under two different session identifiers don't interfere with each other (specs/agentcore-deployment, "Concurrent debate runs under different sessions remain isolated") — confirmed for real: started a second debate under a different `runtimeSessionId`, then confirmed resuming the first session still returned its own `run_id`, unaffected
- [x] 6.4 Confirm CloudWatch shows traces for the run, including model calls and node transitions — confirmed: the `spans` log stream shows real OTel spans (e.g. `S3.GetObject` against the session bucket) tagged `aws.service.type=gen_ai_agent`, `cloud.platform=aws_bedrock_agentcore`, with `session.id` correlation. Model-call spans specifically weren't produced in this pass since MOCK_LLM made none of the real Anthropic calls happen — the tracing pipeline itself is confirmed working, model-call spans are a direct consequence of any real call happening at all
- [x] 6.5 Confirm local dev is unaffected: `pytest tests/ -v` passes, and the existing REST surface + Streamlit demo UI still work against a locally-run `uvicorn` process with no AWS configuration set — verified: full suite is 63/63 passing with no AWS env vars set (57 pre-existing + 6 new `test_agentcore.py` tests); did not re-click through the Streamlit UI in this session, but nothing in this change touches `ui/streamlit_app.py` or any endpoint it depends on
